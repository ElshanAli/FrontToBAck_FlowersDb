@{
    HomeViewModel homeViewModel = Model;
    var products = homeViewModel.Products;
}

<!-- PRODUCTS START -->

<section id="products">
    <div class="container">
        <h1 style="color:red">Discount Products</h1>
        <div class="row">
            @await Component.InvokeAsync("Discount")

        </div>
    </div>
</section>


<section id="products">
    <div class="container">
        <h1 style="color:green">All Products</h1>
        <div class="row">
            @foreach (var item in products)
            {
                @await Html.PartialAsync("_ProductPartialView", item)
            }

        </div>
    </div>
</section>

<!-- PRODUCTS END -->
